node_modules/
logs/
*.log
.env
.env.*

# Bulk downloads (ficam em D:\brasildados)
downloads/
*.zip
*.7z

# Dados brutos grandes — JSON curados em data/ vão para o repo
data/tse/*.zip

# Gerado em CI/local antes do deploy do Worker
worker/data-bundle.json
